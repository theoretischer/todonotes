<?xml version="1.0" encoding="utf-8"?>
<!--
  Netzwerk-Sicherheits-Policy:
  - Öffentliche Domains (todo.christopherh.de etc.): HTTPS (Android-Default).
  - Lokale Adressen (localhost, 127.0.0.1, 192.168.*, 10.*, 172.16-31.*):
    cleartext HTTP erlaubt, damit man im Dev/WLAN gegen den lokalen
    Backend-Container testen kann, ohne HTTPS-Zertifikat für die LAN-IP.

  Android matcht <domain> als Suffix auf Hostnamen/IPs.
  Sobald die echte Domain per Reverse-Proxy mit HTTPS läuft, greift für sie
  automatisch wieder der strenge HTTPS-Default (cleartextTrafficPermitted=false
  in base-config).
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">10.0.0.0</domain>
        <domain includeSubdomains="true">172.16.0.0</domain>
    </domain-config>
</network-security-config>
