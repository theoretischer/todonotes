<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme (Material3) -->
    <!-- Parent: Theme.Material.NoActionBar (dunkel) damit die ersten
         Systemleisten-Frames im Dark Mode nicht weiss flackern;
         CosSetEdgeToEdge uebernimmt dann die Icon-Farben. -->
    <style name="Theme.TodoNotes" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!-- Theme für die Fullscreen-Alarm-Activity: dunkel, immediate -->
    <style name="Theme.TodoNotes.FullScreen" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowShowWallpaper">true</item>
    </style>
</resources>
